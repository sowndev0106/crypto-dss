import { sharedTypesSharedTypes } from './shared-types-shared-types';

describe('sharedTypesSharedTypes', () => {
  it('should work', () => {
    expect(sharedTypesSharedTypes()).toEqual('shared-types-shared-types');
  });
});
